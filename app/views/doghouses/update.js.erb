<% if @error %>
  alert('There was an error editing this DogHouse entry.');
<% else %>
  $('#doghouse').html("<%= j render(partial: 'doghouse_table') %>");
  window.formatDates();
  window.set_countdowns();
<% end %>