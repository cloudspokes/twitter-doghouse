<% if current_user %>
  Welcome <%= current_user.name %>!
  <%= link_to "Sign Out", signout_path %>
  <ol>
  <% current_user.get_following_users.each do |user| %>
      <li><%= user.screen_name %></li>
  <% end %>
  </ol>
<% else %>
  <%= link_to "Sign in with Twitter", "/auth/twitter" %>
<% end %>